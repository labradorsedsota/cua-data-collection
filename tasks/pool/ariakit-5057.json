{
  "task_id": "ariakit-5057",
  "repo": "ariakit/ariakit",
  "repo_url": "https://github.com/ariakit/ariakit",
  "issue_url": "https://github.com/ariakit/ariakit/issues/5057",
  "issue_title": "Dialog - `data-enter` is never removed from DOM when props are applied to nested DOM element in `render`",
  "issue_body_preview": "### Current behavior\n\nThe `data-enter` attribute is supposed to be added and removed automatically depending on the state of the dialog. While this works correctly in most cases, it fails when using the `render` to apply the props to a nested element, as in the [Dialog with scrollable backdrop](http",
  "fix_pr": 5743,
  "buggy_commit": "2d47020b72",
  "fix_commit": "2cf9c35e0f",
  "pr_changed_files": 6,
  "framework": "react",
  "package_manager": "npm",
  "deploy_commands": [
    "git clone https://github.com/ariakit/ariakit.git ariakit-5057",
    "cd ariakit-5057",
    "git checkout 2d47020b72",
    "npm install",
    "npm run dev"
  ],
  "deploy_verify": "curl -s http://localhost:3000 | grep -q '<'",
  "dev_url": "http://localhost:3000",
  "test_page": "/",
  "app_name": "Ariakit",
  "test_description_zh": "打开一个使用 render prop 将属性应用到嵌套 DOM 元素的 Dialog 组件（如带有可滚动背景的对话框），观察对话框打开后 `data-enter` 属性是否在进入动画完成后正常添加，以及在对话框关闭时 `data-enter` 属性是否被正确移除。",
  "expected_result_zh": "Dialog 打开时，`data-enter` 属性应在进入动画触发后正确添加到对话框元素上；Dialog 关闭后，`data-enter` 属性应被正确移除，不会残留在 DOM 中，确保动画/过渡状态正常切换。",
  "timeout": 600,
  "ground_truth": {
    "bug_type": "",
    "expected_behavior": "",
    "actual_behavior": ""
  },
  "_screening": {
    "ui_observable": true,
    "has_screenshot": false,
    "issue_labels": [
      "bug",
      "has workaround"
    ]
  },
  "_ui_score": 8,
  "_description_source": "llm_api_batch9",
  "backend_risk": false,
  "repo_size_kb": 335647,
  "batch": 9
}